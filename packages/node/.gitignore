# Cargo build output (this crate is standalone — its own target dir).
/target

# napi build artifacts (regenerated by `napi build`): the platform-specific
# native binary, the generated CommonJS loader, and its type definitions.
*.node
/index.js
/index.d.ts

# napi per-target publish packages (generated by `napi create-npm-dirs`).
/npm
