# Cargo build output (this crate is standalone — its own target dir).
/target

# napi build artifacts (regenerated by `napi build --esm`): the ESM binding
# entry, its type definitions, and the platform-specific native binary. The
# Backend adapter (backend.js / backend.d.ts) is hand-written and committed.
/index.js
/index.d.ts
*.node

# napi per-target publish packages (generated by `napi create-npm-dirs`).
/npm
