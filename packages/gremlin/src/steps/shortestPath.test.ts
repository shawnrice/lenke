import { describe, test } from 'bun:test';

// Stubbed tests for shortestPath() — emit shortest path(s) from each source.
// TODO: shortestPath() not yet implemented in v2 (also depends on with()).
describe('shortestPath tests (stubs)', () => {
  // doc: g.V().has('person','name','marko').shortestPath()
  //          .with(ShortestPath.target, __.has('name','josh')) — [v[1],v[4]]
  test.skip('TODO shortestPath: target via with() option', () => {});
});
