{
  "name": "@pl-graph/native",
  "description": "JavaScript/TypeScript bindings to the Rust `pl-graph-core` columnar graph engine, with a single facade over native (FFI) and WebAssembly backends.",
  "license": "Apache-2.0",
  "type": "module",
  "sideEffects": false,
  "types": "./dist/types/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types/index.d.ts",
      "default": "./dist/esm/index.mjs"
    },
    "./ffi": {
      "types": "./dist/types/backend-ffi.d.ts",
      "default": "./dist/esm/backend-ffi.mjs"
    },
    "./wasm": {
      "types": "./dist/types/backend-wasm.d.ts",
      "default": "./dist/esm/backend-wasm.mjs"
    }
  },
  "scripts": {
    "build": "bun build.ts",
    "build:rust": "cargo build --release --manifest-path ../../crates/pl-graph-core/Cargo.toml",
    "build:wasm": "cargo build --release --target wasm32-unknown-unknown --no-default-features --features gql,gremlin,ndjson,codecs,arrow --manifest-path ../../crates/pl-graph-core/Cargo.toml",
    "build:wasm:min": "cargo build --release --target wasm32-unknown-unknown --no-default-features --features gql --manifest-path ../../crates/pl-graph-core/Cargo.toml",
    "test": "bun test"
  },
  "dependencies": {
    "@pl-graph/errors": "workspace:*"
  },
  "devDependencies": {
    "@pl-graph/dev": "workspace:*",
    "@types/bun": "^1.3.13",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^6.0.0"
  }
}
